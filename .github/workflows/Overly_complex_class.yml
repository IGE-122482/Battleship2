#Número de aluno: 122482 - Nome: Leonardo Teodoro
name: Qodana - Overly Complex Class

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  qodana:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Run Qodana
        uses: JetBrains/qodana-action@v2023.3
        env:
          QODANA_TOKEN: ${{ secrets.QODANA_TOKEN }}
