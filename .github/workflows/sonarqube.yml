# Número de aluno: 122457 - Nome: António Namniyek
name: SonarQube Cloud Analysis

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  sonarqube:
    runs-on: ubuntu-latest

    steps:
      
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0

      
      - name: Set up JDK 21
        uses: actions/setup-java@v3
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      
      - name: Verify Java and Maven
        run: |
          java -version
          mvn -version

      
      - name: Build and analyze
        run: |
          mvn -B clean verify sonar:sonar \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.organization=ige-122482 \
            -Dsonar.projectKey=IGE-122482_Battleship2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
