# Número de aluno: 122457 - Nome: António Namniyek
name: SonarQube Cloud Analysis

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  sonarqube:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Necessário para melhor análise

      - name: Configurar JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Cache dos pacotes SonarQube
        uses: actions/cache@v3
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Construir e analisar com Maven
        run: mvn verify sonar:sonar
          -Dsonar.projectKey=SEU_PROJECT_KEY
          -Dsonar.organization=SEU_ORG
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
